# Node basic example

Requires local server (`npm run dev`) and `AI_BRAIN_API_KEY`.

```bash
npm run build:sdk
AI_BRAIN_API_KEY=aic_... node examples/node-basic/index.mjs
```

Uses `@ratary/sdk` only — no direct fetch.
