# Phase 08.7 — Automatic Graph Relation Inference

**Status:** Design draft · ADR-041 Proposed  
**Capability:** Auto-populate `memory_relations` from observation, semantic similarity, project, conversation, temporal proximity, dependency — with weight, confidence, evidence.

**Flag:** `RELATION_INFERENCE_ENABLED=false`
