{
  "title": "AI Brain — Graph",
  "uid": "ai-brain-graph",
  "description": "Graph query duration and adapter health",
  "schemaVersion": 39,
  "version": 1,
  "tags": ["ai-brain", "graph"],
  "panels": [
    {
      "id": 1,
      "title": "Graph Query p99",
      "type": "timeseries",
      "targets": [{ "expr": "histogram_quantile(0.99, rate(ai_brain_graph_query_duration_seconds_bucket[5m]))" }]
    }
  ]
}
