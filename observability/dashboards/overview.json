{
  "title": "Ratary — Overview",
  "uid": "ai-brain-overview",
  "description": "Golden signals: latency, traffic, errors, saturation",
  "schemaVersion": 39,
  "version": 1,
  "tags": ["ai-brain", "overview"],
  "panels": [
    {
      "id": 1,
      "title": "HTTP Request Rate",
      "type": "timeseries",
      "targets": [{ "expr": "rate(ratary_http_requests_total[5m])" }]
    },
    {
      "id": 2,
      "title": "HTTP p99 Latency",
      "type": "timeseries",
      "targets": [{ "expr": "histogram_quantile(0.99, rate(ratary_http_request_duration_seconds_bucket[5m]))" }]
    }
  ]
}
