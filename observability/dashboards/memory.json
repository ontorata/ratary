{
  "title": "Ratary — Memory",
  "uid": "ai-brain-memory",
  "description": "Memory CRUD rates, search latency, error ratio",
  "schemaVersion": 39,
  "version": 1,
  "tags": ["ai-brain", "memory"],
  "panels": [
    {
      "id": 1,
      "title": "Memory Operations",
      "type": "timeseries",
      "targets": [{ "expr": "rate(ratary_memory_operations_total[5m])" }]
    },
    {
      "id": 2,
      "title": "Search p99 Latency",
      "type": "timeseries",
      "targets": [{ "expr": "histogram_quantile(0.99, rate(ratary_memory_search_duration_seconds_bucket[5m]))" }]
    }
  ]
}
