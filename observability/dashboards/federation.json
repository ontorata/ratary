{
  "title": "Ratary — Federation",
  "uid": "ai-brain-federation",
  "description": "Peer sync lag, egress, conflict signals",
  "schemaVersion": 39,
  "version": 1,
  "tags": ["ai-brain", "federation"],
  "panels": [
    {
      "id": 1,
      "title": "Federation Sync Lag",
      "type": "timeseries",
      "targets": [{ "expr": "ratary_federation_sync_lag_seconds" }]
    },
    {
      "id": 2,
      "title": "Federation Egress",
      "type": "timeseries",
      "targets": [{ "expr": "rate(ratary_federation_egress_bytes_total[5m])" }]
    }
  ]
}
