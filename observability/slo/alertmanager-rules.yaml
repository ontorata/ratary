groups:
  - name: ai-brain-slo-alerts
    rules:
      - alert: HighHTTPErrorRate
        expr: sum(rate(ratary_http_requests_total{status=~"5.."}[5m])) / sum(rate(ratary_http_requests_total[5m])) > 0.05
        for: 5m
        labels:
          severity: critical
        annotations:
          summary: HTTP 5xx error rate above 5%
      - alert: HighHTTPLatencyP99
        expr: histogram_quantile(0.99, sum(rate(ratary_http_request_duration_seconds_bucket[5m])) by (le)) > 2
        for: 10m
        labels:
          severity: warning
        annotations:
          summary: HTTP p99 latency above 2s
      - alert: EmbeddingFailureSpike
        expr: sum(rate(ratary_embedding_requests_total{status="error"}[5m])) > 0.1
        for: 5m
        labels:
          severity: warning
        annotations:
          summary: Embedding error rate elevated
