{
  "name": "@ai-brain/cli",
  "version": "1.0.0",
  "description": "CLI for AI Memory Cloud — uses @ai-brain/sdk only (Phase 16)",
  "type": "module",
  "bin": {
    "ai-brain": "./dist/main.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@ai-brain/sdk": "1.0.0"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
