# OpenAPI SSOT — Phase 16

| File | Purpose |
|------|---------|
| `ai-brain-v1.openapi.json` | Curated SDK v1 surface (memory, context, capabilities, ecosystem) |
| `openapi.snapshot.json` | Full spec from running server (`npm run snapshot:openapi`) |

Generate language SDKs: `npm run generate:sdks`
