# Phase 38 — Code Memory indexer dry-run evidence

**Date:** 2026-07-29  
**Command:** `npm run index:code -- --owner=<uuid> --repository=ontorata/ratary --root=. --commit=9abd0ad`  
**Host:** Owner laptop · repo `ai-brain` @ `9abd0ad`  
**Flag:** `CODE_MEMORY_ENABLED=false` · `CODE_STORE_PROVIDER=none`  
**Mode:** dry-run (nothing persisted)

## Result

| Field | Value |
|-------|--------|
| dryRun | `true` |
| enabled | `false` |
| indexerVersion | `ts-api-1.0` |
| filesScanned | **947** |
| nodes | **12746** |
| edges | **22070** |
| runId | `null` (flag off) |

## Interpretation

- Extract path works on the Ratary monorepo without enabling production writes.
- I0 preserved: flag remains off; no D1/Postgres code_* mutation from this run.
- Ops pack / `index:code:execute` still **blocked** until Owner authorizes flag-on staging prove.

## Next (Owner)

1. Ops pack for staging flag-on prove (migrations + execute on a small fixture)  
2. Or Hold — keep Code Memory off in production  
