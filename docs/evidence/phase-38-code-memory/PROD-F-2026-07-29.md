# Phase 38 F — Production Code Memory enable

**Date:** 2026-07-29  
**Status:** **Accepted / Complete**  
**Authority:** Owner re-open (“selesaikan Phase 38 F”) · prior Hold ARCH-0253 · [F-READINESS-HOLD.md](./F-READINESS-HOLD.md) · [OPS-PACK.md](./OPS-PACK.md)

## Owner Accept of F

| Item | State |
|------|--------|
| Hold lifted | Explicit Owner request to complete Phase 38 F |
| First index set | Fixture only (`fixture/phase38`) — not full monorepo |
| Prod D1 | `ai-cloud` (`1d01e219…`) |
| Hosted | Vercel Production `ratary` · `https://ratary.ontorata.com` |

## Preconditions checklist

1. [x] Owner explicit Accept of F  
2. [x] Prod D1 migrated (`npm run db:migrate` against prod env)  
3. [x] Vercel Production: `CODE_MEMORY_ENABLED=true` · `CODE_STORE_PROVIDER=sql`  
4. [x] Agreed first index set = fixture (small)  
5. [x] Rollback: set Vercel flag `false` (I0: unused on recall)  
6. [x] Monitor smoke: D1 run + service ports + hosted REST traverse  

## Execute report

| Field | Value |
|-------|--------|
| dryRun | `false` |
| enabled | `true` |
| indexerVersion | `ts-api-1.0` |
| filesScanned / nodes / edges | 2 / 8 / 13 |
| runId | `dde4542d-24ac-4f18-a239-b06838bc295b` |
| run status (D1) | `completed` |
| Vercel deploy | `dpl_HRetqxGDYMTjSTG59S7SvyWS5gyW` (READY) |

## Read-path prove

### Service layer (prod D1 ports)

| Check | Result |
|-------|--------|
| Ports enabled | PASS (`CODE_MEMORY_ENABLED=true`) |
| `getByStableKey` `file:fixture/phase38:src/greet.ts` | PASS |
| `listNeighbors` depth=1 both | 8 edges |
| `listByRepo` | 8 nodes |

### Hosted REST (production)

| Check | Result |
|-------|--------|
| `GET /api/v1/graph/capabilities` | `supportsCodeGraph=true` · `codeTraversalEnabled=true` |
| `GET /api/v1/graph/code/nodes/:id` | PASS · kind `file` · `src/greet.ts` |
| `POST /api/v1/graph/code/traverse` (`codeNodeId` / `stableKey`) | PASS · neighbors=4 · nodeIds=5 |

## Rollback

```text
CODE_MEMORY_ENABLED=false
# optional: CODE_STORE_PROVIDER=none
```

Tables may remain; recall ignores Code Memory when flag off (I0).

## Explicit non-claims

1. Full monorepo production index was **not** run (fixture-only day-1).  
2. Laptop default `.env` may still keep Code Memory **off**; CLI ops use process env override.  
3. This does **not** claim SOC 2 Type I/II or Availability certification.
