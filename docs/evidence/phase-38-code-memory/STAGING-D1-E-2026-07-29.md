# Phase 38 — Staging D1 prove (E)

**Date:** 2026-07-29  
**Pack:** [OPS-PACK.md](./OPS-PACK.md)  
**Authority:** ARCH-0251 / NOTE-0120 · Owner OK create `ratary-staging`  
**Owner decision (2026-07-29):** **Accept E** · then **Hold** (do not run F)  
**Production flag:** remains `CODE_MEMORY_ENABLED=false` on prod / laptop `.env`

## Environment

| Field | Value |
|-------|--------|
| D1 name | `ratary-staging` |
| D1 id (prefix) | `72947c9f…` |
| Region | APAC |
| Env file | `.env.staging` (gitignored) — **not** production `.env` |
| Flags (staging only) | `CODE_MEMORY_ENABLED=true` · `CODE_STORE_PROVIDER=sql` · `SQL_PROVIDER=d1` |
| Fixture | `docs/evidence/phase-38-code-memory/fixture/` |
| Repository label | `fixture/phase38` |

## Steps executed

1. `wrangler d1 create ratary-staging` → new DB (not `ai-cloud`).
2. `.env.staging` from `.env` with staging `D1_DATABASE_ID` + code-memory flags.
3. Shell overrides: `D1_DATABASE_ID` set **before** process start (dotenv does not override).
4. `npm run db:migrate` → success against staging.
5. `npm run index:code:execute -- --owner=<MCP_OWNER_ID> --repository=fixture/phase38 --root=…/fixture`

## Execute report

| Field | Value |
|-------|--------|
| dryRun | `false` |
| enabled | `true` |
| indexerVersion | `ts-api-1.0` |
| filesScanned / nodes / edges | 2 / 8 / 13 |
| runId | `53db0bbe-5bfc-44b5-a492-4d3d31bdfd73` |
| run status (D1) | `completed` |

## Read-path prove (staging D1 ports)

Service-layer against staging D1 (not hosted production MCP):

| Check | Result |
|-------|--------|
| `listByRepo` (repo UUID) | 8 nodes |
| `getNode` (file `src/greet.ts`) | PASS |
| `traverse` depth=1 both | enabled · 4 neighbors · 5 nodeIds |

## Isolation

| Check | Result |
|-------|--------|
| Prod D1 `ai-cloud` fixture nodes (`stable_key` like `fixture/phase38`) | **0** |
| Staging counts | nodes=8 · edges=13 · runs=1 |

## Explicit non-claims

1. This is **not** production enable (**F** still blocked).
2. Hosted MCP/REST `traverse_code` on production Ratary was **not** run (flag remains off there).
3. Laptop default `.env` still points at prod D1 with Code Memory off.

## Owner Accept + Hold

| Decision | Detail |
|----------|--------|
| **Accept E** | Staging D1 prove accepted as sufficient for write-path validation |
| **Hold** | Do **not** enable production Code Memory (F deferred) |
| Steady state | Prod / Vercel: `CODE_MEMORY_ENABLED=false` · staging D1 retained for future F |

## Remaining (after F)

- Full-monorepo / additional repo production indexes — Owner-agreed set only (fixture-first F is done).
- VPS e2e operational delta — separate track.
